<div class="row">
  <div class='col-md-2'>
    <%= link_to 'Créer une ligue', new_tournament_ligue_path, class: "btn btn-primary" %>
  </div>
</div><br />


<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ligues.each do |ligue| %>
      <tr>
        <td><%= ligue.name %></td>
        <% if user_signed_in? && current_user.id == ligue.owner_id %>
            <td><%= link_to 'Show', tournament_ligue_path(ligue.tournament_id, ligue.id) %></td>
            <td><%= link_to 'Edit', edit_tournament_ligue_path(ligue.tournament_id,ligue) %></td>
            <td><%= link_to 'Destroy', tournament_ligues_path(ligue.tournament_id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <%else%>
            <td><%= link_to "DEMANDEZ VOTRE ADHESION",
                            tournament_ligue_path(ligue.tournament_id, ligue.id) , :class => 'btn btn-success btn-mediumlarge' %></td>
            <td></td>
            <td></td>
        <%end%>
      </tr>
    <% end %>
  </tbody>
</table>